---
# === PRIMARY CONFIGURATION ===
selected_disk_name: ""
partition_strategy: "use_whole_disk"  # use_whole_disk, create_partition

# === PARTITION CONFIGURATION ===
split_disk: false                     # true/false - split disk into 2 partitions
partition_size_gb: 10                 # Size for first partition (when split_disk=true)

# === VOLUME GROUP CONFIGURATION ===
volume_group_name: ""                 # Primary VG name
volume_group2_name: ""                # Secondary VG name (when split_disk=true)

# === FILESYSTEM CONFIGURATION ===
filesystem_config: []                 # Primary VG filesystems
filesystem_config2: []                # Secondary VG filesystems (when split_disk=true)

# === SYSTEM SETTINGS ===
lvm_debug: true
auto_generate_lv_names: true
force_recreate_partitions: false