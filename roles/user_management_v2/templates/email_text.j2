Dear {{ requester_name }},

Your user account creation request SR{{ sr_number }} has been successfully completed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 SERVICE REQUEST SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SR Number    : {{ sr_number }}
Requester    : {{ requester_name }}
Contact      : {{ requester_tel }}
Email        : {{ requester_email }}
Completed At : {{ completion_time }} ({{ timezone_display }})

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
👥 USER ACCOUNTS CREATED ({{ users | length }})
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{% for user in users %}
Username : {{ user.name }}
  UID     : {{ user.uid | default('auto-assigned') }}
  Purpose : {{ user.purpose | default('Not specified') }}
  Home    : {{ user.home_directory | default('/home/' + user.name) }}
  Groups  : {{ (user.groups | default([]) | join(', ')) if user.groups else 'N/A' }}
  Password: {% if user.random_password %}{{ user.password }}   ⚠️ Please store this securely.{% else %}As provided{% endif %}

{% endfor %}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ STATUS : Completed Successfully
🕐 Timestamp: {{ completion_time }} (GMT+7)

Thank you,
{{ email_brand_name }}
{{ email_brand_org }}

---------------------------------------------------------------------
This is an automated message. Please do not reply to this email.
Generated on: {{ completion_date }} at {{ completion_time_only }} {{ timezone_display }}
